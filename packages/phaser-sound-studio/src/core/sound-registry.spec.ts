import { describe, expect, it } from 'vitest';

import { SoundRegistry } from './sound-registry';

describe('SoundRegistry', () => {
    it('should be defined', () => {
        expect(SoundRegistry).toBeDefined();
    });
});
